# Forge OS

A 10-persona AI development platform. 105 specialized agents. 35 slash commands. One unified build methodology.

## What Is This

Forge OS is a standalone Tauri v2 desktop application — a native program where you direct AI agents to build software. It provides:

- **10 personas** with persistent identities, self-awareness, and accumulated failure modes
- **10 intelligences** that handle recon, regression testing, red-teaming, impact analysis, and monitoring
- **10 orchestrators** that coordinate multi-persona quality gates
- **10 utilities** for scaffolding, documentation, dependency audits, and more
- **35 sub-agents** for focused checks (accessibility, security, performance, etc.)
- **35 slash commands** that automate common workflows
- **A spec-first build methodology** with 34 rules and 9 documented failure modes

You describe what you want to build. The team builds it, verifies it, and learns from every mistake.

## Quickstart

```bash
git clone https://github.com/CYM4TIC/forge-OS.git
cd forge-OS
claude
```

When Claude starts, Nyx (the build orchestrator) introduces the platform and walks you through setup.

Two paths:
- **`/init`** — Start a new project from scratch. Guided flow: discovery, architecture decisions, spec generation, build planning.
- **`/link /path/to/repo`** — Connect an existing codebase. Agents analyze it, you confirm, full system activates.

## Recommended MCP Connections

| MCP | What It Unlocks | Who Uses It |
|-----|----------------|-------------|
| **GitHub** | Push code, read files, search codebases | Every agent |
| **Database** (Supabase, etc.) | Live schema queries, migrations, policy audits | Scout, Kehinde, Tanaka |
| **Preview** | Browser verification, screenshots, accessibility | Mara, Riven, Wraith |
| **Browser** (Chrome) | Deep automation, red-team testing | Wraith |

Start with GitHub + your database. Add the rest as you need them.

## The Team

| Persona | Domain | What They Do |
|---------|--------|-------------|
| **Nyx** | Build | Translates specs into production code |
| **Pierce** | QA | Checks every line against the spec |
| **Mara** | UX | Audits usability, accessibility, responsiveness |
| **Riven** | Design | Enforces design tokens, touch targets, themes |
| **Kehinde** | Systems | Thinks in failure modes and data integrity |
| **Tanaka** | Security | RLS policies, auth gates, input validation |
| **Vane** | Finance | Payment flows, rate calculations, audit trails |
| **Voss** | Legal | Compliance, consent, regulatory boundaries |
| **Calloway** | Growth | Pricing, positioning, competitive strategy |
| **Sable** | Brand | Voice consistency, copy quality, tone |

## Philosophy

- **The operator never writes code.** Nyx produces everything.
- **The builder never grades their own work.** External agents verify every batch.
- **Failure modes persist.** Every mistake makes the team permanently smarter.
- **Specs are law.** Architecture decisions are locked. The spec doesn't negotiate.
- **Context is managed.** 5-tier activation loads only what's needed. Handoffs preserve continuity.

---

*Extracted from the Cathedral — 57 batches, 326 build steps, 9 failure modes discovered, 0 repeated.*
